import { IconsHi2, MuiIcon } from "../../utils/RenderIcons";
import { Th, Tr } from "../Table/MyTableEngine";

export const HeadRow = (props) => {
  const { data, sortBy, sortOrder, sortThisColumn } = [props];
  return (
    <>
      <p>s</p>
    </>
  );
};
