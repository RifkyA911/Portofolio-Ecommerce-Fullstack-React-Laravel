// export const SidebarHandler = (key) => {
//   setCurrent(key); // active current page
//   screenWidth < 1024 ? dispatch(toggleSidebar(false)) : undefined; // responsive utilites for sidebar autohide
// };
